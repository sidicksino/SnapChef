# Task Tracking: Phase 3 (Mobile App Frontend)

- `[x]` **Initialize App:** Create Expo project named `frontend`.
- `[x]` **Move Model:** Place `best_int8.tflite` into the `assets` folder.
- `[x]` **Setup NativeWind:** Install and configure Tailwind CSS for React Native.
- `[x]` **Setup Vision Camera:** Install `react-native-vision-camera` and `react-native-fast-tflite`.
- `[x]` **UI: Splash & Onboarding:** Build the blue-blur premium splash and onboarding screens.
- `[/]` **UI: Auth:** Build the Login and Sign Up screens (backend wiring lands with the Integrate API task).
- `[ ]` **UI: Home & Scan:** Build the magazine feed home screen and the AR scan screen.
- `[ ]` **UI: History & Profile:** Build the masonry gallery and Spotify-style profile.
- `[ ]` **Integrate API:** Connect all frontend actions to the FastAPI backend using Axios + JWT.
