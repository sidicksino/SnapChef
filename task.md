# Task Tracking: Phase 3 (Mobile App Frontend)

- `[x]` **Initialize App:** Create Expo project named `frontend`.
- `[x]` **Move Model:** Place `best_int8.tflite` into the `assets` folder.
- `[x]` **Setup NativeWind:** Install and configure Tailwind CSS for React Native.
- `[x]` **Setup Vision Camera:** Install `react-native-vision-camera` and `react-native-fast-tflite`.
- `[x]` **UI: Splash & Onboarding:** Build the blue-blur premium splash and onboarding screens.
- `[x]` **UI: Auth:** Login and Sign Up screens, now wired to the real backend (see Integrate API below).
- `[/]` **UI: Home & Scan:** Magazine feed home screen (real recipe data) and a real camera capture flow are built, plus a manual ingredient-entry flow as a working alternative. Still open: wiring camera captures through the on-device YOLOv8 model (`react-native-fast-tflite` + `best_int8.tflite`) for live ingredient detection — the scan screen currently just captures a photo, no on-device detection yet.
- `[x]` **UI: History & Profile:** Real 5-tab bottom nav (Home/Search/Scan/Saved/Profile), the Saved masonry gallery (real recipe data), and the Spotify-style Profile screen (real account data, stats, dietary preferences) are all built.
- `[x]` **Integrate API:** Axios client (`src/lib/api-client.ts`) with JWT auth (stored via `expo-secure-store` on native, `localStorage` on web — SecureStore's web build is a no-op stub) wired end to end: register/login/logout with real error messages, GET/PUT `/api/users/me` (profile + dietary preferences), GET `/api/recipes` (Home + Saved), and a new manual-entry screen (`generate-manual.tsx`) hitting the real `POST /api/recipes/generate` + `POST /api/recipes` so the whole loop works without waiting on camera detection. Verified end-to-end against the real local backend (not mocked) via Playwright on web; native verified for network reachability and code-path parity, not interactively (no UI automation available in this environment to type into native text fields). `OPENAI_API_KEY` isn't configured anywhere in this environment, so `/generate` calls return a real 500 from the backend — confirmed that error surfaces correctly to the user rather than failing silently; add a key to `backend/.env` to actually get recipes back.
